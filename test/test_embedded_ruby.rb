require "helper"

# <- @items.each do |item| ->
# <li <= item => to show non/printing ruby li>
# <- end ->

describe "embedded ruby" do
  it "handles <- non printing ruby ->"
  it "handles <= printing ruby =>"
end
